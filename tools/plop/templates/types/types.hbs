/** @file {{pascalCase name}} module type declarations */

import type {{camelCase name}}Mutations from "./{{camelCase name}}.mutations.ts";
import type {{camelCase name}}Queries from "./{{camelCase name}}.queries.ts";
import type {{camelCase name}}Repository from "./{{camelCase name}}.repository.ts";

declare module "@fastify/awilix" {
  interface Cradle {
    {{camelCase name}}Mutations: ReturnType<typeof {{camelCase name}}Mutations>;
    {{camelCase name}}Queries: ReturnType<typeof {{camelCase name}}Queries>;
    {{camelCase name}}Repository: ReturnType<typeof {{camelCase name}}Repository>;
  }
}
